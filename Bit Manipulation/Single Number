class Solution {
    public int singleNumber(int[] nums) {
        //any number with XOR 0 is number itself
        int result = 0;
        
        for(int num : nums){
            result ^= num;
        }

        return result;
    }
}